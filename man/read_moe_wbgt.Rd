% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_moe_wbgt.R
\name{read_moe_wbgt}
\alias{read_moe_wbgt}
\title{Read a WBGT forecast or observation CSV from the Ministry of the Environment}
\usage{
read_moe_wbgt(
  type,
  station = NULL,
  station_no = NULL,
  prefecture = NULL,
  year_month = NULL,
  .download = TRUE
)
}
\arguments{
\item{type}{\code{"forecast"} for predicted values or \code{"observe"} for observed
ones.}

\item{station}{One of the 11 monitoring sites, capitalised in romaji
(\code{"Sapporo"}, \code{"Tokyo"}, \code{"Naha"}, ...). Used for the \code{mntr} series.}

\item{station_no}{Site number as used by the Japan Meteorological Agency,
for example \code{"43056"}.}

\item{prefecture}{Prefecture in lower-case romaji, for example \code{"tokyo"}.
The accepted spellings are the \code{roman} column of the bundled
\code{wbgt_observe*.csv} tables.}

\item{year_month}{Target month as \code{"YYYYMM"}. Required for \code{type = "observe"}.}

\item{.download}{Whether to also save the CSV into the working directory
under its own file name. Parsing reads from the URL either way.}
}
\value{
A tibble of WBGT values, or \code{NULL} when the supplied combination of
arguments matches no branch. The absent \verb{else} is a known gap.
}
\description{
Builds the download URL for one of the seasonal CSV files published by the
Ministry of the Environment heat illness prevention information site, then
parses it. Which file is requested depends on \code{type} and on which of the
location arguments are supplied; see \code{\link[=moe_wbgt_request_url]{moe_wbgt_request_url()}} for the
combinations.
}
\details{
This targets the CSV download service that predates the 2026 WebAPI. Data
provision is seasonal, so a call outside the operating period fails at the
download rather than returning an empty table.
}
\examples{
\dontrun{
read_moe_wbgt(type = "forecast", station_no = "43056")
read_moe_wbgt(type = "observe", station_no = "43056", year_month = "202404")
}

}
\seealso{
\code{\link[=moe_wbgt_request_url]{moe_wbgt_request_url()}}, \code{\link[=parse_moe_wbgt_csv]{parse_moe_wbgt_csv()}}
}
