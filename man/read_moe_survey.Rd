% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_moe_survey.R
\name{read_moe_survey}
\alias{read_moe_survey}
\title{Read observed WBGT values from the Ministry of the Environment WebAPI}
\usage{
read_moe_survey(
  station_no = NULL,
  pref_cd = NULL,
  date_from,
  date_to,
  data_type = c(0L, 1L),
  max_span = NULL
)
}
\arguments{
\item{station_no}{A vector of AMeDAS site numbers. Mutually exclusive with
\code{pref_cd}; when both are \code{NULL}, all sites are requested.}

\item{pref_cd}{A vector of the API's JMA-region prefecture codes. See
\link{wbgt_pref_codes}. Mutually exclusive with \code{station_no}.}

\item{date_from, date_to}{Observation range. Date, POSIXct, and parseable
character values are accepted and interpreted in Japan Standard Time.}

\item{data_type}{One or both of \code{0} (estimated) and \code{1} (measured).}

\item{max_span}{Optional initial request width, as a positive number of
seconds or a \code{difftime}. The default sends the full range first and only
splits after a limit error.}
}
\value{
A tibble containing the upstream fields. \code{wbgt_WO}, \code{wbgt_Tw}, and
\code{wbgt_Tg} are degrees Celsius; \code{wbgt_WI} is the 0--4 data-quality code.
Values are converted from JSON strings to numeric but are otherwise
unchanged.
}
\description{
Retrieves estimated or measured observed values by site, JMA-region
prefecture code, or all sites. Requests are split recursively when the API
reports that the 25,000-record limit was exceeded.
}
\examples{
\dontrun{
read_moe_survey(
  station_no = 44132,
  date_from = "2026-09-01",
  date_to = "2026-09-02"
)
}
}
