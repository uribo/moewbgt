% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_moe_wbgt.R
\name{moe_wbgt_request_url}
\alias{moe_wbgt_request_url}
\title{Build the download URL for a WBGT CSV}
\usage{
moe_wbgt_request_url(
  type,
  station_no = NULL,
  prefecture = NULL,
  station = NULL,
  year_month = NULL
)
}
\arguments{
\item{type}{\code{"forecast"} for predicted values or \code{"observe"} for observed
ones.}

\item{station_no}{Site number as used by the Japan Meteorological Agency,
for example \code{"43056"}.}

\item{prefecture}{Prefecture in lower-case romaji, for example \code{"tokyo"}.
The accepted spellings are the \code{roman} column of the bundled
\code{wbgt_observe*.csv} tables.}

\item{station}{One of the 11 monitoring sites, capitalised in romaji
(\code{"Sapporo"}, \code{"Tokyo"}, \code{"Naha"}, ...). Used for the \code{mntr} series.}

\item{year_month}{Target month as \code{"YYYYMM"}. Required for \code{type = "observe"}.}
}
\value{
A length-one glue string with the URL, or \code{NULL} when no branch
matches. The function has no final \verb{else}, so an unsupported combination
is silent rather than an error.
}
\description{
Assembles the URL of one seasonal CSV. The path prefix identifies the
series:
}
\details{
\describe{
\item{\verb{prev15WG/dl/}}{forecast values}
\item{\verb{est15WG/dl/}}{observed values for the current year}
\item{\verb{mntr/dl/}}{the 11 monitoring sites, keyed by \code{station}}
\item{\verb{mntr/final/\{year\}/}}{confirmed values for an earlier year}
}

The branch is chosen from \code{type} together with which location arguments are
non-\code{NULL}, and whether \code{year_month} falls in the current year.
}
\examples{
moe_wbgt_request_url(type = "forecast", station_no = "43056")
}
\seealso{
\code{\link[=moe_wbgt_request_urls]{moe_wbgt_request_urls()}} for the vectorised form
}
