% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_moe_wbgt.R
\name{parse_moe_wbgt_csv}
\alias{parse_moe_wbgt_csv}
\title{Parse a WBGT CSV into a long tibble}
\usage{
parse_moe_wbgt_csv(path, file_type, .station_no = NULL, .station = NULL)
}
\arguments{
\item{path}{URL or path of the CSV.}

\item{file_type}{File layout in the manual's notation: one of \code{"1-A"},
\code{"1-B"}, \code{"1-C"}, \code{"2-A"}, \code{"2-B"}, \code{"2-C"}, \code{"2-D"}.}

\item{.station_no}{Site number for \code{"2-A"}. When \code{NULL} it is recovered
from the file name.}

\item{.station}{Monitoring site name for \code{"2-D"}. When \code{NULL} it is
recovered from the file name.}
}
\value{
A tibble whose first column is \code{type} (\code{"forecast"} or
\code{"observe"}), followed by the site identifier, the time columns and
\code{wbgt}.
}
\description{
Reads one of the CSV layouts described in the Ministry of the Environment
data service manual and returns it in long form. \code{path} may be a URL or a
local file.
}
\details{
The layouts differ enough that the parser branches on \code{file_type} rather
than inspecting the file:

\describe{
\item{\code{"1-A"}, \code{"1-B"}, \code{"1-C"}}{forecast values, one column per
forecast datetime; pivoted to \code{datetime} and \code{wbgt}}
\item{\code{"2-A"}}{observed values for a single site, already long}
\item{\code{"2-B"}, \code{"2-C"}}{observed values with one column per site;
pivoted to \code{station_no} and \code{wbgt}}
\item{\code{"2-D"}}{observed values for one monitoring site, including the
globe temperature column \code{tg}}
}

Values are read as-is with no unit conversion. Whether the upstream columns
are degrees Celsius or tenths is not yet settled; see \code{PROVENANCE.md}.
}
\examples{
\dontrun{
parse_moe_wbgt_csv("yohou_43056.csv", file_type = "1-A")
}

}
\seealso{
\code{\link[=read_moe_wbgt]{read_moe_wbgt()}}
}
