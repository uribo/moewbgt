% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_moe_forecast.R
\name{read_moe_forecast}
\alias{read_moe_forecast}
\title{Read WBGT forecasts from the Ministry of the Environment WebAPI}
\usage{
read_moe_forecast(
  station_no = NULL,
  pref_cd = NULL,
  date_from = NULL,
  date_to = NULL,
  origin_date = NULL,
  fixed_time = NULL,
  max_span = NULL
)
}
\arguments{
\item{station_no}{A vector of AMeDAS site numbers. Mutually exclusive with
\code{pref_cd}; when both are \code{NULL}, all sites are requested.}

\item{pref_cd}{A vector of the API's JMA-region prefecture codes. See
\link{wbgt_pref_codes}. Mutually exclusive with \code{station_no}.}

\item{date_from, date_to}{Range of forecast issue times. Date, POSIXct, and
parseable character values are accepted and interpreted in Japan
Standard Time. Supply both or neither.}

\item{origin_date}{One forecast issue time. Mutually exclusive with
\code{date_from} and \code{date_to}.}

\item{fixed_time}{Optional issue time in \code{"HHMMSS"} format for range
searches.}

\item{max_span}{Optional initial request width, as a positive number of
seconds or a \code{difftime}. The default sends the full range first and only
splits after a limit error.}
}
\value{
A tibble with \code{reference_time}, \code{wbgt_no}, \code{forecast_val},
\code{forecast_time}, and \code{flag}. \code{forecast_val} is the upstream integer value
in degrees Celsius multiplied by 10 and is not converted by this
function.
}
\description{
Retrieves forecast values by site, JMA-region prefecture code, or all
sites. Range searches are split recursively when the API reports that the
25,000-record limit was exceeded.
}
\examples{
\dontrun{
read_moe_forecast(station_no = 44132, origin_date = "2026-09-01 00:00:00")
}
}
